@model decimal

<div class="form-group">
    @Html.LabelFor(m => m)
    @Html.TextBoxFor(m => m, "{0:0.00}", new { @class = "form-control", @type = "number", @step = "0.01" })
    @Html.ValidationMessageFor(m => m, "", new { @class = "text-danger" })
</div>